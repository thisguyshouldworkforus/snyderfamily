---
  - name: SnyderFamilyINT Playbook
    hosts: all
    remote_user: root
    become: true
    become_method: sudo
    roles: base

  - name: Default DNS Playbook
    hosts: dns
    role: dns

  - name: Default Media Playbook
    hosts: media
    tasks:
      import_tasks: media.yml
